# TODO

- [x] Fix Axios 404 by updating silent refresh URL in `src/app/lib/axios.js` to use the configured `api` instance: `api.post('/auth/refresh', ...)`.
- [x] Run dev server and trigger token refresh to confirm 200 response.


